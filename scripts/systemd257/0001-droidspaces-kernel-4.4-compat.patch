diff --git a/src/basic/cgroup-util.c b/src/basic/cgroup-util.c
index 9b3c7c0..f02e38b 100644
--- a/src/basic/cgroup-util.c
+++ b/src/basic/cgroup-util.c
@@ -14,6 +14,7 @@
 #include "cgroup-util.h"
 #include "constants.h"
 #include "dirent-util.h"
+#include "env-util.h"
 #include "extract-word.h"
 #include "fd-util.h"
 #include "fileio.h"
@@ -40,6 +41,10 @@
 #include "user-util.h"
 #include "xattr-util.h"
 
+bool cg_droidspaces_compat_enabled(void) {
+        return getenv_bool("SYSTEMD_DROIDSPACES_COMPAT") > 0;
+}
+
 int cg_path_open(const char *controller, const char *path) {
         _cleanup_free_ char *fs = NULL;
         int r;
diff --git a/src/basic/cgroup-util.h b/src/basic/cgroup-util.h
index 9d7573d..ad008e8 100644
--- a/src/basic/cgroup-util.h
+++ b/src/basic/cgroup-util.h
@@ -313,6 +313,7 @@ int cg_all_unified(void);
 int cg_hybrid_unified(void);
 int cg_unified_controller(const char *controller);
 int cg_unified_cached(bool flush);
+bool cg_droidspaces_compat_enabled(void);
 static inline int cg_unified(void) {
         return cg_unified_cached(true);
 }
diff --git a/src/core/cgroup.c b/src/core/cgroup.c
index b1981f5..ed8ea3f 100644
--- a/src/core/cgroup.c
+++ b/src/core/cgroup.c
@@ -2876,6 +2876,18 @@ static int unit_update_cgroup(
         if (!UNIT_HAS_CGROUP_CONTEXT(u))
                 return 0;
 
+        if (cg_droidspaces_compat_enabled()) {
+                r = unit_pick_cgroup_path(u);
+                if (r < 0)
+                        return r;
+
+                CGroupRuntime *crt = ASSERT_PTR(unit_get_cgroup_runtime(u));
+                crt->cgroup_realized = true;
+                crt->cgroup_realized_mask = 0;
+                crt->cgroup_enabled_mask = 0;
+                return 0;
+        }
+
         if (u->freezer_state != FREEZER_RUNNING)
                 return log_unit_error_errno(u, SYNTHETIC_ERRNO(EBUSY), "Cannot realize cgroup for frozen unit.");
 
@@ -3011,6 +3023,9 @@ int unit_attach_pids_to_cgroup(Unit *u, Set *pids, const char *suffix_path) {
         if (set_isempty(pids))
                 return 0;
 
+        if (cg_droidspaces_compat_enabled())
+                return 0;
+
         /* Load any custom firewall BPF programs here once to test if they are existing and actually loadable.
          * Fail here early since later errors in the call chain unit_realize_cgroup to cgroup_context_apply are ignored. */
         r = bpf_firewall_load_custom(u);
@@ -3819,6 +3834,9 @@ void unit_add_to_cgroup_empty_queue(Unit *u) {
 
         assert(u);
 
+        if (cg_droidspaces_compat_enabled())
+                return;
+
         /* Note that there are four different ways how cgroup empty events reach us:
          *
          * 1. On the unified hierarchy we get an inotify event on the cgroup
@@ -4166,6 +4184,17 @@ int manager_setup_cgroup(Manager *m) {
 
         assert(m);
 
+        if (cg_droidspaces_compat_enabled()) {
+                m->cgroup_root = mfree(m->cgroup_root);
+                m->cgroup_root = strdup("");
+                if (!m->cgroup_root)
+                        return -ENOMEM;
+
+                m->cgroup_supported = 0;
+                log_notice("Droidspaces compatibility mode: running without cgroup management.");
+                return 0;
+        }
+
         /* 1. Determine hierarchy */
         m->cgroup_root = mfree(m->cgroup_root);
         r = cg_pid_get_path(SYSTEMD_CGROUP_CONTROLLER, 0, &m->cgroup_root);
diff --git a/src/core/exec-invoke.c b/src/core/exec-invoke.c
index 4e9228e..8f84644 100644
--- a/src/core/exec-invoke.c
+++ b/src/core/exec-invoke.c
@@ -4767,6 +4767,10 @@ int exec_invoke(
         /* We need sandboxing if the caller asked us to apply it and the command isn't explicitly excepted
          * from it. */
         needs_sandboxing = (params->flags & EXEC_APPLY_SANDBOXING) && !(command->flags & EXEC_COMMAND_FULLY_PRIVILEGED);
+        if (needs_sandboxing && cg_droidspaces_compat_enabled()) {
+                log_exec_debug(context, params, "Droidspaces compatibility mode: disabling unit sandboxing on this kernel.");
+                needs_sandboxing = false;
+        }
 
         if (params->cgroup_path) {
                 /* If delegation is enabled we'll pass ownership of the cgroup to the user of the new process. On cgroup v1
diff --git a/src/core/main.c b/src/core/main.c
index 1e105ca..a79b478 100644
--- a/src/core/main.c
+++ b/src/core/main.c
@@ -3199,8 +3199,12 @@ int main(int argc, char *argv[]) {
                 if (!skip_setup) {
                         r = mount_cgroup_legacy_controllers(loaded_policy);
                         if (r < 0) {
-                                error_message = "Failed to mount cgroup v1 hierarchy";
-                                goto finish;
+                                if (cg_droidspaces_compat_enabled())
+                                        log_warning_errno(r, "Droidspaces compatibility mode: failed to mount cgroup hierarchy, ignoring: %m");
+                                else {
+                                        error_message = "Failed to mount cgroup v1 hierarchy";
+                                        goto finish;
+                                }
                         }
                 }
 
diff --git a/src/core/manager.c b/src/core/manager.c
index aadefe5..3056f05 100644
--- a/src/core/manager.c
+++ b/src/core/manager.c
@@ -1148,6 +1148,9 @@ static int manager_setup_cgroups_agent(Manager *m) {
         };
         int r;
 
+        if (cg_droidspaces_compat_enabled())
+                return 0;
+
         /* This creates a listening socket we receive cgroups agent messages on. We do not use D-Bus for delivering
          * these messages from the cgroups agent binary to PID 1, as the cgroups agent binary is very short-living, and
          * each instance of it needs a new D-Bus connection. Since D-Bus connections are SOCK_STREAM/AF_UNIX, on
diff --git a/src/core/unit.c b/src/core/unit.c
index 7843f48..8a43614 100644
--- a/src/core/unit.c
+++ b/src/core/unit.c
@@ -4097,7 +4097,7 @@ int unit_kill(
          * resource, and we shouldn't allow us to be subjects for such allocation sprees) */
         if (IN_SET(whom, KILL_ALL, KILL_ALL_FAIL) && code == SI_USER) {
                 CGroupRuntime *crt = unit_get_cgroup_runtime(u);
-                if (crt && crt->cgroup_path) {
+                if (crt && crt->cgroup_path && !cg_droidspaces_compat_enabled()) {
                         _cleanup_set_free_ Set *pid_set = NULL;
 
                         if (signo == SIGKILL) {
@@ -4875,7 +4875,7 @@ int unit_kill_context(Unit *u, KillOperation k) {
         wait_for_exit = wait_for_exit || r > 0;
 
         CGroupRuntime *crt = unit_get_cgroup_runtime(u);
-        if (crt && crt->cgroup_path &&
+        if (crt && crt->cgroup_path && !cg_droidspaces_compat_enabled() &&
             (c->kill_mode == KILL_CONTROL_GROUP || (c->kill_mode == KILL_MIXED && k == KILL_KILL))) {
                 _cleanup_set_free_ Set *pid_set = NULL;
 
@@ -5420,7 +5420,8 @@ int unit_set_exec_params(Unit *u, ExecParameters *p) {
 
         /* Copy parameters from unit */
         CGroupRuntime *crt = unit_get_cgroup_runtime(u);
-        p->cgroup_path = crt ? crt->cgroup_path : NULL;
+        p->cgroup_path = cg_droidspaces_compat_enabled() ? NULL :
+                crt ? crt->cgroup_path : NULL;
         SET_FLAG(p->flags, EXEC_CGROUP_DELEGATE, unit_cgroup_delegate(u));
 
         p->received_credentials_directory = u->manager->received_credentials_directory;
