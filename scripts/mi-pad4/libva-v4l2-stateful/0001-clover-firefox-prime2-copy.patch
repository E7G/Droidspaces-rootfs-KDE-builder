diff --git a/src/vabackend.c b/src/vabackend.c
index a0be460..bbf6e5a 100644
--- a/src/vabackend.c
+++ b/src/vabackend.c
@@ -1109,7 +1109,9 @@ static VAStatus v4l2_DestroyImage(
     VADriverContextP ctx,
     VAImageID image)
 {
-    return VA_STATUS_SUCCESS;
+    /* DeriveImage allocates a normal buffer id. The old no-op leaked one id
+     * per copied frame and eventually exhausted MAX_BUFFERS during playback. */
+    return v4l2_DestroyBuffer(ctx, image);
 }
 
 static VAStatus v4l2_SetImagePalette(
